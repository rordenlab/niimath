{
  "name": "@niivue/niimath",
  "version": "1.3.3",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./niimath.js": "./dist/niimath.js"
  },
  "scripts": {
    "test": "bun test ./tests",
    "test:wasi:fixtures": "bun run scripts/wasi-fixtures.ts",
    "test:wasi": "make -C ../src && bun run makeWasi && bun run scripts/wasi-fixtures.ts --require-builds && bun test ./src/wasi",
    "build": "bun run esbuild.config.ts",
    "make": "make -C ../src",
    "makeWasm": "make wasm -C ../src",
    "makeWasi": "make wasm-wasi -C ../src",
    "makeWasmEmccCore": "make wasm-emcc-core -C ../src",
    "wasiNodeSmoke": "bun run scripts/wasi-node-run.ts",
    "wasiChromium": "bun run scripts/wasi-chromium.ts",
    "makeWasmGpl": "git submodule update --init ../src/GPL && make wasm GPL=1 WASM_OUT=../js/src/niimath-gpl.js -C ../src && bun run scripts/pre-build.ts -i src/niimath-gpl.js -o src/niimath-gpl.js",
    "parseHelpText": "bun run scripts/parseNiimathHelp.ts",
    "generateTypes": "bun run scripts/generateTypeDefinitions.ts",
    "prebuild": "bun run make && bun run parseHelpText && bun run generateTypes && bun run makeWasm && bun run scripts/pre-build.ts -i src/niimath.js -o src/niimath.js",
    "dev": "bun run build && bun run server.ts",
    "pub": "bun run build && npm publish --access public"
  },
  "keywords": [
    "niivue",
    "niimath",
    "nifti",
    "medical",
    "imaging",
    "brain"
  ],
  "author": "NiiVue developers",
  "license": "BSD-2-Clause",
  "description": "A javascript library to easily use the WASM build of Chris Rorden's niimath command line program written in C",
  "files": [
    "dist/",
    "LICENSE"
  ],
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "esbuild": "^0.28.0",
    "playwright": "^1.61.1",
    "typescript": "^5.7.2"
  }
}
